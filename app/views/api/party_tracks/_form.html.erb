<%= form_for(@party_track) do |f| %>
  <% if @party_track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@party_track.errors.count, "error") %> prohibited this party_track from being saved:</h2>

      <ul>
      <% @party_track.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :played_at %><br />
    <%= f.datetime_select :played_at %>
  </div>
  <div class="field">
    <%= f.label :references %><br />
    <%= f.text_field :references %>
  </div>
  <div class="field">
    <%= f.label :references %><br />
    <%= f.text_field :references %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
