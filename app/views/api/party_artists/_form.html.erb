<%= form_for(@party_artist) do |f| %>
  <% if @party_artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@party_artist.errors.count, "error") %> prohibited this party_artist from being saved:</h2>

      <ul>
      <% @party_artist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
